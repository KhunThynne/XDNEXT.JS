import Loading from "@/shared/components/LoadingComponent";

export default function AuthLoading() {
  return (
    <Loading description="Welcome to authentication" className={`bg-accent!`} />
  );
}
